default_stages: [commit]
repos:
    - repo: https://github.com/antonbabenko/pre-commit-terraform
      rev: v1.77.1
      hooks:
          - id: terraform_fmt
            args:
              - --args=-recursive
              - --args=-check
          - id: terraform_validate
